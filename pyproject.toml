[project]
name = "imapmigrate"
version = "0.1.0"
description = ""
authors = [
    { name = "Шулаков Иван", email = "ivanshulakov9@gmail.com" }
]
license = { text = "MIT" }
requires-python = ">=3.11,<3.13"
dependencies = [
    "ruff>=0.16.2,<0.17.0",
    "imapclient (>=3.1.0,<4.0.0)",
    "tqdm (>=4.70.0,<5.0.0)",
    "dotenv (>=0.9.9,<0.10.0)",
    "loguru (>=0.7.3,<0.8.0)"
]

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"